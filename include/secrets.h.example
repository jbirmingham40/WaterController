#pragma once

// Copy this file to secrets.h (which is gitignored) and fill in your own
// values. secrets.h is included by src/main.cpp.
//
// WiFi network credentials are no longer compiled in here - they're set (and
// tested/confirmed) from the device's own settings web page and stored in
// NVS. This password gates that settings web page. The device's setup
// access point itself is open/unauthenticated by design; this password only
// applies once a browser reaches the settings page.
static const char *SETTINGS_PASSWORD = "your-settings-password-here";
